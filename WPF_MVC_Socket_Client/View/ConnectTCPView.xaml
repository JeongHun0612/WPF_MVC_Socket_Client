<UserControl x:Class="WPF_MVC_Socket_Client.View.ConnectTCPView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:WPF_MVC_Socket_Client.View"
             mc:Ignorable="d" 
             FontFamily="/Fonts/#Roboto Medium"
             d:DesignHeight="100" d:DesignWidth="200">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="23"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- IP Address TextBox -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="IP" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="13" FontFamily="#Roboto Light"/>
        <view:IPMaskedTextBox Grid.Row="0" Grid.Column="1" Height="25" Margin="10,0,0,0"/>

        <!-- Port Number TextBox -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Port" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="13" FontFamily="#Roboto Light"/>
        <TextBox Grid.Row="1" Grid.Column="1" Width="50" Height="25" VerticalContentAlignment="Center" MaxLength="4" HorizontalAlignment="Left" Margin="10,0,0,0"/>

        <!-- Connect Button -->
        <Rectangle Grid.Row="1" Grid.Column="1" Width="20" Height="20" Fill="{StaticResource ContentBorderBrush}" Stroke="{StaticResource ContentBackgroundBrush}" RadiusX="3" RadiusY="3" Margin="0,0,5,0"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Connect" Width="70" Height="25" HorizontalAlignment="Right" FontFamily="/Fonts/#Roboto Light" />
    </Grid>
</UserControl>
