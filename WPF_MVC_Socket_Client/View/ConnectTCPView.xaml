<UserControl x:Class="WPF_MVC_Socket_Client.View.ConnectTCPView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:WPF_MVC_Socket_Client.View"
             mc:Ignorable="d" 
             FontFamily="/Fonts/#Roboto Medium"
             d:DesignHeight="100" d:DesignWidth="200">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- IP Address TextBox -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="IP" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <view:IPMaskedTextBox Grid.Row="0" Grid.Column="1" Height="20" Margin="5,0,0,0"/>

        <!-- Port Number TextBox -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Port" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Style="{DynamicResource TextBoxStyle}" Width="50" Height="20" VerticalContentAlignment="Center" MaxLength="4" HorizontalAlignment="Left" Margin="5,0,0,0"/>

        <!-- Connect Button -->
        <Button Grid.Row="1" Grid.Column="1" Style="{DynamicResource ButtonStyle}" Content="Connect" Width="70" Height="21" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
