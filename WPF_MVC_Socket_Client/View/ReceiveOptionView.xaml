<UserControl x:Class="WPF_MVC_Socket_Client.View.ReceiveOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVC_Socket_Client.View"
             mc:Ignorable="d" 
             d:DesignHeight="470" d:DesignWidth="250">
    
    <GroupBox Header="송수신 설정" Foreground="{StaticResource ContentForegroundBrush}" BorderBrush="{StaticResource ContentBorderBrush}" BorderThickness="0.1" FontFamily="/Fonts/#KoPubDotum Medium" Margin="8">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" VerticalAlignment="Center">
                <TextBlock Text="문자열 출력 방식" FontSize="11"/>
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <RadioButton GroupName="StringPrintMethod" Content="HEX" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center" FontSize="11" FontFamily="/Fonts/#Roboto Medium"/>
                    <RadioButton GroupName="StringPrintMethod" Content="ASCII" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center" FontSize="11" FontFamily="/Fonts/#Roboto Medium" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="송수신 출력 기능" FontSize="11"/>
                <WrapPanel Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="시간 표시" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center" FontSize="11" FontFamily="/Fonts/#Roboto Medium"/>
                        <CheckBox Content="RX/TX 표시" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center"  FontSize="11" FontFamily="/Fonts/#Roboto Medium" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="일시 정지" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center" FontSize="11" FontFamily="/Fonts/#Roboto Medium" Margin="0,5,0,0"/>
                        <CheckBox Content="자동 보내기" Foreground="{StaticResource ContentForegroundBrush}" VerticalContentAlignment="Center"  FontSize="11" FontFamily="/Fonts/#Roboto Medium" Margin="10,5,0,0"/>
                    </StackPanel>
                </WrapPanel>
            </Grid>
            <Grid Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="송수신 출력 표시" FontSize="11"/>
                <StackPanel Margin="0,20,0,0">
                    <CheckBox Content="송신 문자열 표시" Foreground="{StaticResource ContentForegroundBrush}" FontSize="11" FontFamily="/Fonts/#Roboto Medium"/>
                    <CheckBox Content="송신 문자열 표시" Foreground="{StaticResource ContentForegroundBrush}" FontSize="11" FontFamily="/Fonts/#Roboto Medium" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>
