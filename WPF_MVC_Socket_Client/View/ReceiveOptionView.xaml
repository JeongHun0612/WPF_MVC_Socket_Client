<UserControl x:Class="WPF_MVC_Socket_Client.View.ReceiveOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVC_Socket_Client.View"
             mc:Ignorable="d" 
             d:DesignHeight="470" d:DesignWidth="250">

    <GroupBox Header="송수신 설정" Foreground="{StaticResource ContentForegroundBrush}" BorderBrush="{StaticResource ContentBorderBrush}" BorderThickness="0.1" FontSize="14" FontFamily="/Fonts/#KoPubDotum Medium" Margin="8">
        <Grid Margin="10">
            <StackPanel>
                <!-- 데이터 출력 방식 -->
                <Grid Margin="0,20,0,0">
                    <TextBlock Text="데이터 출력 방식" FontSize="13" FontFamily="/Fonts/#KoPubDotum Light"/>
                    <WrapPanel Margin="5,25,0,0">
                        <RadioButton GroupName="StringPrintMethod" Content="HEX"/>
                        <RadioButton GroupName="StringPrintMethod" Content="ASCII" Margin="10,0,0,0"/>
                    </WrapPanel>
                </Grid>

                <!-- 데이터 출력 기능 -->
                <Grid Margin="0,30,0,0">
                    <TextBlock Text="데이터 출력 기능" FontSize="13" FontFamily="/Fonts/#KoPubDotum Light"/>
                    <WrapPanel Margin="5,25,0,0">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Content="시간 표시"/>
                            <CheckBox Content="RX/TX 표시" Margin="10,0,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Content="일시 정지" Margin="0,8,0,0"/>
                            <CheckBox Content="자동 보내기" Margin="10,8,0,0"/>
                        </StackPanel>
                    </WrapPanel>
                </Grid>

                <!-- 데이터 출력 표시 -->
                <Grid Margin="0,30,0,0">
                    <TextBlock Text="데이터 출력 표시" FontSize="13" FontFamily="/Fonts/#KoPubDotum Light"/>
                    <StackPanel Margin="5,25,0,0">
                        <CheckBox Content="송신 [RX]" IsChecked="True" Foreground="{StaticResource ContentForegroundBrush}" FontSize="13" FontFamily="/Fonts/#Roboto Light"/>
                        <CheckBox Content="수신 [TX]" IsChecked="True" Foreground="{StaticResource ContentForegroundBrush}" FontSize="13" FontFamily="/Fonts/#Roboto Light" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- 데이터 관리 -->
                <Grid Margin="0,30,0,0">
                    <TextBlock Text="데이터 관리" FontSize="13" FontFamily="/Fonts/#KoPubDotum Light"/>
                    <WrapPanel Margin="5,25,0,0">
                        <Button Content="저장" Width="80" Height="25"/>
                        <Button Style="{DynamicResource ErrorButtonStyle}" Content="삭제" Width="80" Height="25" Margin="10,0,0,0"/>
                    </WrapPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </GroupBox>
</UserControl>
